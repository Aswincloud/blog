# Copy to .secrets-denylist (gitignored) and add your exact secret strings,
# one per line, to hard-block them from ever being committed.
# The pre-commit hook checks staged files against these literals.
your-wifi-password
ghp_your_github_token
your-api-keys-etc
